import 'package:flutter/painting.dart';

const large = 24.0;

const medium = EdgeInsets.all(large/3);

const small = EdgeInsets.all(large/6);

const vertical = EdgeInsets.symmetric(horizontal: large/6, vertical: large/2);