import 'package:flutter/painting.dart';

const base = 24.0;

const medium = EdgeInsets.all(base/3);

const small = EdgeInsets.all(base/6);

const vertical = EdgeInsets.symmetric(horizontal: base/6, vertical: base/2);