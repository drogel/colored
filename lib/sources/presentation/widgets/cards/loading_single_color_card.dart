import 'package:flutter/material.dart';

class LoadingSingleColorCard extends StatelessWidget {
  const LoadingSingleColorCard({Key key}) : super(key: key);

  // TODO(754824): add variables and methods to LoadingSingleColorCard

  @override
  Widget build(BuildContext context) {
    // TODO(754824): define widget build method in LoadingSingleColorCard
    return null;
  }
}